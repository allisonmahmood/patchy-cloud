<!-- PROTOTYPE for #314 -->

# `patchy init --tier 2` to a published two-artifact version (prototype)

Throwaway vertical slice for [#314](https://github.com/allisonmahmood/patchy-cloud/issues/314) on the tier 2 map (#294). It implements the server function contract (#296) far enough that a fresh agent can go from `patchy init --tier 2` to a version whose page calls a query and a mutation through the real shell and the execution engine (#298). Never merged. Every new or changed file carries a `PROTOTYPE for #314` comment; `git grep "PROTOTYPE for #314"` is the blast radius.

## What is where

| Where                                                | What changed and why                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/execution/` (new)                          | The engine as one Effect service: `loader.ts` (the spike's loader Worker and workerd config as embedded strings), `Engine.ts` (spawns workerd, `bind`, `invoke`, `inspect` in a throwaway process, the loopback callback listener with per-invocation capabilities), `ServerCall.ts` (the `server.call` runtime handler: tier gate, descriptor validation, callbacks to sibling handlers under the admitted binding), `workerd.ts` (binary resolution). |
| `packages/api/src/handlers.ts` (new)                 | Handler descriptor grammar, `checkValue` (args before, result after), `canonicalHandlers` for the server-side comparison.                                                                                                                                                                                                                                                                                                                               |
| `packages/api/src/{runtime,schemas}.ts`              | `server.call` operation and reply shape, codes `handler_failed` and `handler_timeout`, `Manifest.handlers`, `PublishRequest.server`, `GenerateRequest.serverModules`.                                                                                                                                                                                                                                                                                   |
| `packages/patchy/src/server.ts` (new)                | `patchy/server`: `query`/`mutation`/`action`, `bind<Config>()`, `Context`, `HandlerError`, the typed server client. `config.ts` grows `t.object/array/enum/nullable/row` and `Infer`. `handlerError.ts` is shared with the client.                                                                                                                                                                                                                      |
| `packages/patchy/src/serverBuild.ts` (new)           | The server bundle through the repo's Vite 8 (rolldown) library build, the generated guest entry, the import-check plugin for both graphs, discovery through `Engine.inspectBundle`.                                                                                                                                                                                                                                                                     |
| `packages/patchy/src/{repoBuild,commands}.ts`        | Tier 2 publish: build both artifacts, import check on both, handlers into the manifest, `server` on the request. `init --tier 2`.                                                                                                                                                                                                                                                                                                                       |
| `packages/patchy/src/initProject.ts`                 | Tier 2 scaffold: `server/notes.ts`, a vanilla `src/main.ts` calling `patchy.server.notes.*`, `workerd` pinned in `devDependencies`, `server` in `tsconfig`, `AGENTS.md` lines.                                                                                                                                                                                                                                                                          |
| `packages/patchy/src/client.ts`                      | `patchy.server.<module>.<export>(args)`, `isHandlerError`, the `server.call` operation.                                                                                                                                                                                                                                                                                                                                                                 |
| `packages/patchy/src/dev{Server,Resources,Watch}.ts` | `patchy dev` on the engine: server bundle build-watch, inspect + bind per rebuild, the loaded version swapped whole, two loopback listeners (owner `/dev/<patch>`, colleague `/dev-as-colleague/<patch>`), `colleagueUrl` in `dev --json`.                                                                                                                                                                                                              |
| `packages/sdk/src/{Generation,generateClient}.ts`    | Tier 2 generation emits `patchy/_generated/server.ts` (builders bound to `typeof config`), types `patchy.server` from `typeof import("../../server/<m>")`, adds the `patchy-server` skill.                                                                                                                                                                                                                                                              |
| `packages/sdk/skills/patchy-server/SKILL.md` (new)   | The project skill: kinds, context, `HandlerError`, the import rule with its message, the two-viewer dev loop, the transaction gap. `patchy-loop` gains a `server/` line.                                                                                                                                                                                                                                                                                |
| `packages/patches/src/*`                             | Migration `0009_patches_server_artifact` (`server_object_key`, `server_hash`), the second object stored under its own intent, `readServer`, `LoadedVersion.server`, publish admission for tier 2: bundle required, re-derived handlers compared canonically, `invalid_manifest` on disagreement or a bundle that throws or never finishes loading.                                                                                                      |
| `packages/runtime/src/LoadedVersions.ts`             | `server?: { objectKey, digest }` on the loaded version.                                                                                                                                                                                                                                                                                                                                                                                                 |
| `apps/server/src/Server.ts`                          | One `Engine` per server process; `server.call` beside the table handlers with the bundle read from the content store; `PatchesApi` gets the engine for discovery.                                                                                                                                                                                                                                                                                       |
| `pnpm-workspace.yaml`, `eslint.config.js`            | `workerd` build script off (the platform package ships the binary); the CLI's dev surfaces may import `@patchy/execution`.                                                                                                                                                                                                                                                                                                                              |

## Try it

From the worktree, with the Clerk development keys loaded as `docs/DEVELOPMENT.md` says (or a dotenv with an offline `CLERK_JWT_KEY`, which is what the smoke used):

```sh
pnpm install
pnpm dev                                   # builds the patchy tarball, starts Postgres + server + one workerd
set -a; . .local/dev/env; set +a
mkdir -p /tmp/t2 && cd /tmp/t2
node <worktree>/packages/patchy/dist/index.js init notes --tier 2 --purpose "Team notes" --json
cd notes && pnpm exec tsc --noEmit         # the scaffold typechecks
node <worktree>/packages/patchy/dist/index.js dev --json   # url (owner) and colleagueUrl (fixture colleague)
node <worktree>/packages/patchy/dist/index.js publish --json
```

Open the published address in a signed-in browser: the page lists notes through `patchy.server.notes.list()` and adds one through `patchy.server.notes.add({ title })`; an empty title comes back as the handler's `empty_title` error. Add `import { z } from "zod"` to `server/notes.ts` (with `zod` installed so `tsc` passes) and publish again to see the import check refuse. `pnpm dev stop` and `patchy dev stop` clean up.

## What works (measured 2026-09-25, one laptop, embedded Postgres, PGlite in dev)

| Step                           | Result and numbers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 `init --tier 2`              | 8.9 s wall (network install of the tarball, Vite, workerd); the tree typechecks in 1.1 s; skills `patchy-files, patchy-loop, patchy-server, patchy-tables`.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2 contract                     | `query`/`mutation`/`action` from `_generated/server.ts`, `t.object/array/enum/nullable/row`, `args` and `result` required (a declaration without one throws at module init and publish refuses it), `HandlerError`, context narrowed by kind. Nested descriptors validate; unknown fields, missing fields, wrong enum values and unknown handlers answer `invalid_request` in 4-5 ms.                                                                                                                                                                                                                      |
| 3 server bundle + import check | Repo's Vite 8 in library mode (no second toolchain). `import { z } from "zod"` in `server/notes.ts`, in `lib/helper.ts` reached from `server/`, and in `src/helper.ts` reached from `src/` are each refused at module-graph time, naming the importer, with `copy it into \`src/\` or ask Patchy for the capability`. A non-handler export refuses publish with `server/ export probe.helper is not a handler`.                                                                                                                                                                                            |
| 4 engine                       | spawn 41 ms; bind 7-12 ms; warm invoke with one callback 3-5 ms average over 20 (engine only). Guest `fetch("https://example.com")` answers 403 from the refusing loopback. A callback after return: `callback refused: invocation ended`; after the deadline: `capability_refused`. Two viewers' concurrent invocations see their own `ctx.viewer`.                                                                                                                                                                                                                                                       |
| 5 wire + shell                 | `server.call` admitted by the unchanged broker (it keys on `runtimeOperations`); a handler error is `{ ok:false, source:"handler", code, details }` inside a successful reply and `isHandlerError(e, "empty_title")` narrows it on the page. A query that writes: `access_denied` from the callback host. Handler throw: `handler_failed` 500, message only in the log. Bad result: `handler_failed`, `$.count: expected an integer` in the log. `sleep(12000)`: `handler_timeout` at 10.01 s.                                                                                                             |
| 6 `patchy dev`                 | start 4.6 s; first server bundle inspected + bound in 71 ms (bind 11 ms); edit `server/notes.ts` to rebound 122 ms; a `sleep(4000)` in flight across the rebuild finished on its old binding (`slept 4000 then listed 1`) while the next call ran the new bundle. Owner and colleague tabs ready together in 231 ms; simultaneous adds 151/149 ms; `dev.log` shows `[dev@patchy.local]` and `[colleague@patchy.local]` on their own `notes.add` lines. Warm dev calls over PGlite: list 15-36 ms, add 24-32 ms wall. An import refusal from the watcher is logged and the last bound bundle keeps serving. |
| 7 two-artifact publish         | 1.6 s wall: `.html` (11 KB) and `.server.js` (8.9 KB) stored, `server_hash` equals the stored bytes' sha256, `manifest.handlers` recorded. Served page: viewer rendered 121 ms after navigation; click to re-rendered list 66-73 ms; server-side `notes.list` 5-12 ms, `notes.add` 4-6 ms; `runtime_calls` rows carry `usr_dev`, `server.call`, `notes.add`. Raw publish: disagreeing manifest 422 in 48-104 ms; top-level throw 422 in 48 ms (`Uncaught Error: init boom`); `for(;;){}` 422 after 5041 ms and the throwaway workerd is gone; tier 2 without a bundle 422; control 200 in 91 ms.           |
| 9 skills                       | `patchy-server` served by generation on tier 2, `patchy-loop` names `server/`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

`--json` on a tier 2 publish is the tier 1 document unchanged (no handler count); on `dev` it gains `colleagueUrl`.

## Transactions: the stated gap

A mutation's writes are not atomic across callbacks. `probe.writeThenThrow` inserted a row and then threw `HandlerError("after_write")`: the client got the handler error and the row `persist?` stayed in the company database. The skill says so. Holding one company transaction across callbacks is #310's design and was not bolted on.

## What stalled or was skipped

- **Preact scaffold (step 8)**: not done. The client is vanilla TypeScript, so this slice does not prove the #296 scaffold recipe (`jsx: react-jsx`, `oxc.jsx.importSource`, `patchy/preact` with compat).
- **Stopping guest execution**: a handler that sleeps past the deadline is refused correctly, but nothing kills a CPU-spinning handler in the serving engine (no watchdog by decision). A `for(;;){}` _inside a handler_ would stall the one serving workerd for that instance; not run against the instance for that reason. Publish-time `for(;;){}` at module top level is handled because inspection uses a throwaway process.
- **`ctx.shared`, `ctx.files`, `ctx.connections`, `ctx.run`, the mutation key, subscriptions, `useQuery`**: not in this slice.
- **`ctx.log` in the cloud**: lines are `Effect.logInfo` with the correlation id, not columns on `runtime_calls`.
- **Existing tests**: `packages/sdk/src/SdkApi.test.ts` asserts the package's export list and now sees `./server` (expected red on this branch, not touched). The offline suite was interrupted by the host once while running beside typecheck and lint; the rerun status is in the PR body.
- **`pnpm lint`**: status in the PR body.

## Design notes for the map

- **Errors do not cross workerd RPC with their properties.** The loader's `Callbacks` stub threw an `Error` with a `patchyRefusal` field; the guest received a bare `Error`, so a refused write from a query surfaced as `handler_failed`. Fixed by returning refusals as data and rethrowing in the guest entry. #298's "the guest holds an RPC stub" should say the stub answers with a result, not a throw.
- **A handler error is a successful reply**, so the `runtime_calls` row for `empty_title` says `outcome = success`. Decide whether handler errors get their own outcome or ride as success with a code.
- **The generated client needs the `server/` file list**, so adding a file needs `patchy refresh`; a changed export or schema does not (type-only import). #296 point 10 says "no regeneration step"; it holds for edits, not for new files.
- **`optional` on an argument field means the key may be omitted; null needs `t.nullable`.** On table columns `optional` means nullable. Two meanings for one word; the skill states it.
- **Discovery re-derives on the exact stored bytes** (a throwaway workerd per publish, ~45 ms; 5 s bound); the comparison is order-independent JSON. The disagreement message lists handler names only, so a kind or shape mismatch with identical names reads oddly.
- **Two dev identities are two listeners**, because the broker posts to a fixed `/api/runtime/call` and a base path cannot select a principal without a browser-supplied field. The listener is the mount.
- **Vite 8 library mode is enough for the server bundle** (`rolldownOptions.output.codeSplitting = false`); Vite's own `vite/modulepreload-polyfill` import is allowlisted, or `build.modulePreload: false` in the scaffold would remove it.
- **workerd pinned at 1.20260924.1** (a day older than the spike's), so pnpm's `minimumReleaseAge` needed no exclusion; its postinstall is off and the platform package's binary is spawned directly. The patch repo installs it as a devDependency with `--ignore-scripts`.
- **The engine in the server is one process per server**; a rebuilt version is a new worker name (`patch@version#digest`), the bundle is pushed on `bundle_required`.
