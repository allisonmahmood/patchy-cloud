name: Bug report
description: Something in Patchy Cloud isn't working
labels: ["bug", "needs-triage"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for the report.

        **Security vulnerabilities do not belong here.** Report those privately
        through [GitHub Private Vulnerability Reporting](https://github.com/allisonmahmood/patchy-cloud/security/advisories/new).

  - type: textarea
    id: what-happened
    attributes:
      label: What happened?
      description: What did you see, and what did you expect instead?
    validations:
      required: true

  - type: textarea
    id: reproduction
    attributes:
      label: Steps to reproduce
      description: The smallest sequence that reliably shows the problem.
      placeholder: |
        1. Run `patchy ...`
        2. ...
        3. ...
    validations:
      required: true

  - type: dropdown
    id: area
    attributes:
      label: Which part of Patchy Cloud does this affect?
      description: A rough guess is fine, and "Not sure" is a perfectly good answer.
      options:
        - Not sure
        - The CLI (patchy)
        - The server / hosting a published page
        - HTML validation and safety rules
        - Documentation
    validations:
      required: false

  - type: input
    id: cli-version
    attributes:
      label: CLI version
      description: Output of `patchy --version`.
    validations:
      required: false

  - type: input
    id: node-version
    attributes:
      label: Node.js version
      description: Output of `node --version`. Patchy Cloud supports Node 22 and later.
      placeholder: "v22.13.0"
    validations:
      required: false

  - type: textarea
    id: logs
    attributes:
      label: Logs or output
      description: Relevant command output or server logs. This is rendered as code, so no backticks are needed.
      render: shell
    validations:
      required: false

  - type: checkboxes
    id: redaction
    attributes:
      label: Before submitting
      options:
        - label: I have removed API tokens, credentials, and other secrets from everything pasted above.
          required: true
